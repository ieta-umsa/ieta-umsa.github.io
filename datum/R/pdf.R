#install.packages("rJava")
#install.packages("tabulizer",dependencies = "Depends",type = "source")
#install.packages("tabulizerjars",dependencies = "Depends",type = "source")
library(tabulizer)
install.packages("xlsx")
library(xlsx)

################################
#sudo xcode-select --install
#sudo xcode-select -switch /
#sudo R CMD javareconf
################################